del 变量						//回收变量

input("打印的内容:")					//提示用户并让用户输入值，并返回
int(字符变量)						//把字符串转换成 int 类型

print("Hello World!")					//打印字符串
print(u"中文字符")					//中文字符时，需在前面加 u 后程序自动把字符串转为unicode

str(数值变量)						//把数字转换成 str 类型

type(变量)						//打印变量类型